const ReferenceKey = Object.freeze({
    CASCADE: "CASCADE",
    RESTRICT: "RESTRICT",
    SET_DEFAULT: "SET DEFAULT",
    SET_NULL: "SET NULL",
    NO_ACTION: "NO ACTION",
});

module.exports = { ReferenceKey };
